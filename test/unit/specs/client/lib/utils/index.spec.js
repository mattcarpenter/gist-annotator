import utils from 'client/lib/utils';

describe('utils', function () {
  describe('calculatePixelSeconds', function () {
    it('does a thing', function () {
      utils.calculatePixelSeconds([{text:'foo',time:0}],[1]);
      expect(true).to.be.equal(true);
    });
  });
});
